import React, { useState, useEffect } from 'react'; import { ShoppingBag, X, Menu, Instagram, Facebook, Mail, Zap, Droplets, Skull, Star } from 'lucide-react';  // --- Styled Components & Utilities ---  const NeonButton = ({ children, onClick, color = 'cyan', className = '' }) => {   const baseClass = "relative px-6 py-3 font-bold text-black uppercase transition-all duration-200 transform hover:-translate-y-1 active:translate-y-0 group overflow-hidden";   const colorClasses = {     cyan: "bg-cyan-400 hover:bg-cyan-300 shadow-[0_0_20px_rgba(34,211,238,0.6)] hover:shadow-[0_0_40px_rgba(34,211,238,0.8)]",     pink: "bg-fuchsia-500 hover:bg-fuchsia-400 shadow-[0_0_20px_rgba(217,70,239,0.6)] hover:shadow-[0_0_40px_rgba(217,70,239,0.8)]",     white: "bg-white hover:bg-gray-100 shadow-[0_0_20px_rgba(255,255,255,0.4)]"   };    return (     <button onClick={onClick} className={`${baseClass} ${colorClasses[color]} clip-path-slant ${className}`}>       <span className="relative z-10">{children}</span>       <div className="absolute inset-0 bg-white opacity-0 group-hover:opacity-20 transition-opacity"></div>     </button>   ); };  const SectionTitle = ({ title, subtitle }) => (   <div className="text-center mb-16 relative">     <h2 className="text-5xl md:text-7xl font-['Rubik_Wet_Paint'] text-white tracking-widest relative z-10 drop-shadow-[0_5px_5px_rgba(0,0,0,0.8)]">       {title}     </h2>     <h2 className="text-5xl md:text-7xl font-['Rubik_Wet_Paint'] text-cyan-400 absolute top-1 left-1 opacity-60 z-0 blur-[1px]">       {title}     </h2>     <h2 className="text-5xl md:text-7xl font-['Rubik_Wet+Paint'] text-fuchsia-500 absolute top-[-2px] left-[-2px] opacity-60 z-0 blur-[1px]">       {title}     </h2>     {subtitle && <p className="mt-4 text-gray-400 font-mono text-lg uppercase tracking-widest">{subtitle}</p>}     <div className="w-24 h-1 bg-gradient-to-r from-fuchsia-500 via-white to-cyan-400 mx-auto mt-6 rounded-full shadow-[0_0_15px_rgba(255,255,255,0.5)]"></div>   </div> );  // --- Product Data Updated with Image Support ---  const products = [   {     id: 1,     name: "trinket tray",     price: 17,     category: "Trays",     image: "skull", // Fallback icon     // *** START OF IMAGE INCLUSION ***     // The Base64 data for your uploaded 'image.png' (the pink/silver resin piece) has been inserted below.     // Ensure this full data URL (starting with data:image/png;base64,...) is correctly pasted here.     base64Image: "data:image/png;base64, [PASTE THE FULL BASE64 STRING FOR image.png HERE]",      // *** END OF IMAGE INCLUSION ***     description: "handcrafted small oval tray captures glamorous Hollywood style with shimmering, epoxy, finishes, gold accents, and dramatic curves – perfect for organizing jewelry, keys, or coins on your vanity.",     colors: ["#000", "#0ff"]   },   {     id: 2,     name: "Chaos Theory Coasters",     price: 35,     category: "Coasters",     image: "droplet",     description: "Set of 4. Because cup rings on your table are not the kind of chaos we want.",     colors: ["#f0f", "#0ff"]   },   {     id: 3,     name: "Best Friend Keychains",     price: 10,     category: "keychains",     image: "star",     description: "Set of 2 pendants, one for you and your best friend (or fur baby for the antisocial).",     colors: ["#000", "#f0f"]   },   {     id: 4,     name: "Domino Set",     price: 45,     category: "misc",     image: "zap",     description: "totally customizable set of dominoes, because who doesn’t like to play dominoes?? [Optional matching storage box +$10]",     colors: ["#333", "#f00"]   },   {     id: 5,     name: "Switch Plate/Outlet Cover",     price: 10,     category: "Misc",     image: "runes",     description: "Make any room your own with customer covers for every outlet and light switch. Decor switches available as well-double and triple. Each additional switch +$3",     colors: ["#000", "#0f0"]   },   {     id: 6,     name: "definitely NOT a stash jar 💨",     price: 20,     category: "jars",     image: "cloud",     description: "Pastel swirls meeting harsh black geometric lines. Gold flake inclusions.",     colors: ["#f9a", "#0ff"]   } ];  // --- Main App Component ---  export default function App() {   const [cart, setCart] = useState([]);   const [isCartOpen, setIsCartOpen] = useState(false);   const [isMenuOpen, setIsMenuOpen] = useState(false);      // Normalize categories for display (e.g., convert "misc" to "Misc")   const normalizedProducts = products.map(p => ({     ...p,     category: p.category.charAt(0).toUpperCase() + p.category.slice(1).toLowerCase()   }));    const categories = ["All", ...new Set(normalizedProducts.map(p => p.category))];   const [activeCategory, setActiveCategory] = useState("All");      const filteredProducts = activeCategory === "All"      ? normalizedProducts      : normalizedProducts.filter(p => p.category === activeCategory);    const addToCart = (product) => {     setCart([...cart, product]);     setIsCartOpen(true);   };    const removeFromCart = (productId) => {     setCart(cart.filter((item, i) => i !== cart.findIndex(c => c.id === productId)));   };    const cartTotal = cart.reduce((sum, item) => sum + item.price, 0);    // Scroll effect for navbar   const [scrolled, setScrolled] = useState(false);   useEffect(() => {     const handleScroll = () => setScrolled(window.scrollY > 50);     window.addEventListener('scroll', handleScroll);     return () => window.removeEventListener('scroll', handleScroll);   }, []);    return (     <div className="min-h-screen bg-black text-white font-sans selection:bg-fuchsia-500 selection:text-white overflow-x-hidden">       {/* Import Google Fonts */}       <style>         {`           @import url('https://fonts.googleapis.com/css2?family=Rubik+Wet+Paint&family=Permanent+Marker&family=Roboto+Mono:wght@400;700&display=swap');                      .font-grunge { font-family: 'Rubik Wet Paint', cursive; }           .font-marker { font-family: 'Permanent Marker', cursive; }                      .clip-path-slant {             clip-path: polygon(10% 0, 100% 0, 100% 100%, 0% 100%);           }                      .drip-effect {             background-image: radial-gradient(circle at 50% 100%, black 10%, transparent 11%),                               radial-gradient(circle at 50% 100%, black 10%, transparent 11%);             background-size: 20px 20px;             background-position: 0 0, 10px 0;             background-repeat: repeat-x;           }            @keyframes glow {             0%, 100% { text-shadow: 0 0 10px #0ff, 0 0 20px #0ff, 0 0 30px #0ff; }             50% { text-shadow: 0 0 5px #f0f, 0 0 10px #f0f, 0 0 20px #f0f; }           }                      .animate-glow { animation: glow 3s infinite alternate; }                      /* Custom scrollbar */           ::-webkit-scrollbar { width: 8px; }           ::-webkit-scrollbar-track { bg-black; }           ::-webkit-scrollbar-thumb { background: #333; border-radius: 4px; }           ::-webkit-scrollbar-thumb:hover { background: #0ff; }         `}       </style>        {/* --- Navbar --- */}       <nav className={`fixed w-full z-50 transition-all duration-300 border-b ${scrolled ? 'bg-black/90 backdrop-blur-md border-cyan-500/30 py-4' : 'bg-transparent border-transparent py-6'}`}>         <div className="max-w-7xl mx-auto px-4 flex justify-between items-center">           <div className="text-2xl md:text-3xl font-grunge tracking-wider relative group cursor-pointer">             <span className="text-white group-hover:text-cyan-400 transition-colors">HR</span>             <span className="text-fuchsia-500">R</span>           </div>            {/* Desktop Nav */}           <div className="hidden md:flex items-center space-x-8 font-mono text-sm tracking-widest">             <a href="#shop" className="hover:text-cyan-400 transition-colors uppercase">Shop</a>             <a href="#about" className="hover:text-fuchsia-500 transition-colors uppercase">The Process</a>             <a href="#contact" className="hover:text-cyan-400 transition-colors uppercase">Customs</a>             <button onClick={() => setIsCartOpen(true)} className="relative hover:text-fuchsia-500 transition-colors">               <ShoppingBag className="w-6 h-6" />               {cart.length > 0 && (                 <span className="absolute -top-2 -right-2 bg-cyan-400 text-black text-xs font-bold w-5 h-5 rounded-full flex items-center justify-center">                   {cart.length}                 </span>               )}             </button>           </div>            {/* Mobile Menu Btn */}           <div className="md:hidden flex items-center gap-4">              <button onClick={() => setIsCartOpen(true)} className="relative text-white">               <ShoppingBag className="w-6 h-6" />               {cart.length > 0 && (                 <span className="absolute -top-2 -right-2 bg-cyan-400 text-black text-xs font-bold w-5 h-5 rounded-full flex items-center justify-center">                   {cart.length}                 </span>               )}             </button>             <button onClick={() => setIsMenuOpen(!isMenuOpen)} className="text-white">               {isMenuOpen ? <X /> : <Menu />}             </button>           </div>         </div>       </nav>        {/* Mobile Nav Overlay */}       {isMenuOpen && (         <div className="fixed inset-0 z-40 bg-black/95 flex flex-col items-center justify-center space-y-8 font-grunge text-4xl">            <a href="#shop" onClick={() => setIsMenuOpen(false)} className="text-white hover:text-cyan-400">Shop</a>            <a href="#about" onClick={() => setIsMenuOpen(false)} className="text-white hover:text-fuchsia-500">Process</a>            <a href="#contact" onClick={() => setIsMenuOpen(false)} className="text-white hover:text-cyan-400">Customs</a>         </div>       )}        {/* --- Cart Sidebar --- */}       <div className={`fixed inset-y-0 right-0 z-[60] w-full md:w-96 bg-zinc-900 border-l border-fuchsia-500/30 transform transition-transform duration-300 ${isCartOpen ? 'translate-x-0' : 'translate-x-full'}`}>         <div className="p-6 h-full flex flex-col">           <div className="flex justify-between items-center mb-8 border-b border-gray-800 pb-4">             <h2 className="text-2xl font-grunge text-white">Your Stash</h2>             <button onClick={() => setIsCartOpen(false)} className="text-gray-400 hover:text-white"><X /></button>           </div>                      <div className="flex-1 overflow-y-auto space-y-4">             {cart.length === 0 ? (               <p className="text-gray-500 font-mono text-center mt-10">Cart is empty.<br/>Go add some chaos.</p>             ) : (               cart.map((item, idx) => (                 <div key={idx} className="flex bg-black/50 p-4 border border-gray-800 rounded">                    <div className="w-16 h-16 bg-gray-800 rounded flex items-center justify-center text-cyan-400">                      <Star size={20} />                    </div>                    <div className="ml-4 flex-1">                      <h4 className="font-bold text-sm">{item.name}</h4>                      <p className="text-fuchsia-400 font-mono">${item.price}</p>                    </div>                    <button onClick={() => removeFromCart(item.id)} className="text-gray-500 hover:text-red-500">                      <X size={16} />                    </button>                 </div>               ))             )}           </div>            <div className="mt-8 border-t border-gray-800 pt-6">             <div className="flex justify-between text-xl font-bold mb-6 font-mono">               <span>Total</span>               <span>${cartTotal}</span>             </div>             <NeonButton color="cyan" className="w-full">Checkout</NeonButton>           </div>         </div>       </div>        {/* --- Hero Section --- */}       <header className="relative min-h-screen flex items-center justify-center overflow-hidden pt-16">         {/* Background Gradients */}         <div className="absolute top-0 left-0 w-full h-full bg-black z-0">           <div className="absolute top-1/4 left-1/4 w-96 h-96 bg-fuchsia-600/20 rounded-full blur-[100px] animate-pulse"></div>           <div className="absolute bottom-1/4 right-1/4 w-96 h-96 bg-cyan-600/20 rounded-full blur-[100px] animate-pulse" style={{ animationDelay: '1s' }}></div>         </div>          <div className="relative z-10 text-center px-4 max-w-5xl mx-auto">           {/* Main Logo Text Construction */}           <div className="mb-6 relative inline-block">              <h1 className="text-6xl md:text-9xl font-grunge leading-tight">                <span className="block text-white drop-shadow-[0_0_10px_rgba(255,255,255,0.5)]">HOLLYWOOD</span>                <div className="relative inline-block my-2">                  <span className="relative z-10 text-transparent bg-clip-text bg-gradient-to-b from-white to-gray-400" style={{WebkitTextStroke: '2px white'}}>RIOT</span>                  {/* Drip Effects */}                  <Droplets className="absolute -bottom-8 left-1/4 text-cyan-400 w-8 h-8 animate-bounce" style={{ animationDuration: '3s' }} />                  <Droplets className="absolute -bottom-12 right-1/4 text-fuchsia-500 w-6 h-6 animate-bounce" style={{ animationDuration: '4s' }} />                </div>                <span className="block text-white drop-shadow-[0_0_15px_rgba(255,255,255,0.3)]">RESIN</span>              </h1>           </div>                      <p className="text-lg md:text-xl text-gray-300 font-mono tracking-widest mb-10 max-w-2xl mx-auto border-l-2 border-fuchsia-500 pl-4 text-left">             Handmade resin art for the <span className="text-cyan-400">rebels</span>, the <span className="text-fuchsia-500">misfits</span>, and the <span className="text-white">dreamers</span>.           </p>            <div className="flex flex-col md:flex-row gap-6 justify-center">             <NeonButton onClick={() => document.getElementById('shop').scrollIntoView({behavior: 'smooth'})} color="cyan">               Shop The Drop             </NeonButton>             <NeonButton onClick={() => document.getElementById('contact').scrollIntoView({behavior: 'smooth'})} color="pink">               Custom Orders             </NeonButton>           </div>         </div>                  {/* Scroll Indicator */}         <div className="absolute bottom-10 left-1/2 transform -translate-x-1/2 animate-bounce">           <div className="w-1 h-16 bg-gradient-to-b from-cyan-400 to-transparent"></div>         </div>       </header>        {/* --- Shop Section --- */}       <section id="shop" className="py-24 bg-black relative">         <div className="max-w-7xl mx-auto px-4">           <SectionTitle title="The Collection" subtitle="Fresh from the chaos" />            {/* Filters */}           <div className="flex flex-wrap justify-center gap-4 mb-16">             {categories.map(cat => (               <button                 key={cat}                 onClick={() => setActiveCategory(cat)}                 className={`px-6 py-2 font-mono text-sm uppercase tracking-wider border transition-all duration-300 ${                   activeCategory === cat                      ? 'border-cyan-400 text-black bg-cyan-400 shadow-[0_0_15px_rgba(34,211,238,0.5)]'                      : 'border-gray-800 text-gray-400 hover:border-fuchsia-500 hover:text-white'                 }`}               >                 {cat}               </button>             ))}           </div>            {/* Grid */}           <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">             {filteredProducts.map((product) => (               <div key={product.id} className="group relative bg-zinc-900 border border-gray-800 hover:border-fuchsia-500 transition-all duration-500 overflow-hidden">                 {/* Image Placeholder / Actual Image Rendering */}                 <div className="h-64 bg-zinc-800 relative overflow-hidden flex items-center justify-center">                   <div className="absolute inset-0 bg-gradient-to-t from-black to-transparent opacity-60"></div>                                      {product.base64Image ? (                     // Display the Base64 image                     <img                          src={product.base64Image}                          alt={product.name}                          // Added styling for a cover image that scales on hover                         className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"                     />                   ) : (                     // Fallback to the existing Icon representation                     <div className="transform group-hover:scale-110 transition-transform duration-500">                       {product.image === 'skull' && <Skull size={80} className="text-gray-600 group-hover:text-cyan-400 transition-colors duration-300" />}                       {product.image === 'droplet' && <Droplets size={80} className="text-gray-600 group-hover:text-fuchsia-500 transition-colors duration-300" />}                       {product.image === 'zap' && <Zap size={80} className="text-gray-600 group-hover:text-yellow-400 transition-colors duration-300" />}                       {product.image === 'star' && <Star size={80} className="text-gray-600 group-hover:text-purple-400 transition-colors duration-300" />}                       {product.image === 'runes' && <div className="text-6xl text-gray-600 font-serif group-hover:text-green-400">ᚠ</div>}                       {product.image === 'cloud' && <div className="text-6xl text-gray-600 font-serif group-hover:text-pink-300">☁</div>}                     </div>                   )}                    {/* Add to cart overlay */}                   <div className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity duration-300 bg-black/40 backdrop-blur-sm">                     <button                        onClick={() => addToCart(product)}                       className="bg-white text-black font-bold px-6 py-2 uppercase hover:bg-cyan-400 hover:scale-105 transition-all"                     >                       Add to Cart                     </button>                   </div>                 </div>                  {/* Details */}                 <div className="p-6">                   <div className="flex justify-between items-start mb-2">                     <h3 className="text-xl font-bold font-marker tracking-wide">{product.name}</h3>                     <span className="text-cyan-400 font-mono font-bold text-lg">${product.price}</span>                   </div>                   <p className="text-gray-400 text-sm mb-4 leading-relaxed">{product.description}</p>                   <div className="flex gap-2">                     {product.colors.map((c, i) => (                       <div key={i} className="w-4 h-4 rounded-full border border-gray-600" style={{backgroundColor: c}}></div>                     ))}                   </div>                 </div>               </div>             ))}           </div>         </div>       </section>        {/* --- About/Process Section --- */}       <section id="about" className="py-24 bg-zinc-950 relative overflow-hidden">         {/* Decorative Drips */}         <div className="absolute top-0 left-0 w-full h-12 bg-black z-10" style={{clipPath: 'polygon(0 0, 100% 0, 100% 40%, 95% 80%, 90% 40%, 85% 90%, 80% 50%, 75% 100%, 70% 30%, 65% 80%, 60% 40%, 55% 90%, 50% 50%, 45% 100%, 40% 30%, 35% 80%, 30% 40%, 25% 90%, 20% 40%, 15% 100%, 10% 40%, 5% 80%, 0 40%)'}}></div>          <div className="max-w-4xl mx-auto px-4 relative z-20 text-center">           <SectionTitle title="The Method" />                      <div className="grid md:grid-cols-2 gap-12 items-center">             <div className="text-left space-y-6">               <p className="text-gray-300 text-lg leading-relaxed">                 Hollywood Riot Resin isn't about perfection—it's about <span className="text-fuchsia-500 font-bold">expression</span>. Each piece is hand-poured in my studio, embracing the bubbles, the unpredictable swirls, and the chaotic nature of the medium.               </p>               <p className="text-gray-300 text-lg leading-relaxed">                 We use high-grade, UV-resistant epoxy mixed with intense pigments, heavy glitters, and a little bit of attitude. No two pieces are ever the same.               </p>               <div className="pt-4">                 <span className="font-grunge text-4xl text-cyan-400 opacity-80 rotate-3 inline-block">100% HANDMADE</span>               </div>             </div>                          <div className="relative border-2 border-dashed border-gray-700 p-8 rounded-lg transform rotate-2 hover:rotate-0 transition-transform duration-500">                <div className="absolute -top-4 -right-4 w-12 h-12 bg-cyan-400 rounded-full blur-xl opacity-50"></div>                <div className="absolute -bottom-4 -left-4 w-12 h-12 bg-fuchsia-500 rounded-full blur-xl opacity-50"></div>                <h3 className="text-2xl font-marker mb-4 text-white">Why "Riot"?</h3>                <ul className="text-left space-y-4 font-mono text-gray-400">                  <li className="flex items-center"><Zap className="text-yellow-400 mr-3" size={16} /> Breaking standard design rules</li>                  <li className="flex items-center"><Zap className="text-cyan-400 mr-3" size={16} /> Bold, unapologetic colors</li>                  <li className="flex items-center"><Zap className="text-fuchsia-500 mr-3" size={16} /> Functional art that screams</li>                </ul>             </div>           </div>         </div>       </section>        {/* --- Contact Section --- */}       <section id="contact" className="py-24 bg-black relative">         <div className="max-w-3xl mx-auto px-4 text-center">           <SectionTitle title="Get Custom" subtitle="Your vision, my chaos" />                      <div className="bg-zinc-900 border border-gray-800 p-8 md:p-12 shadow-[0_0_50px_rgba(0,0,0,0.5)]">             <form className="space-y-6">               <div className="grid md:grid-cols-2 gap-6">                 <div className="text-left">                   <label className="block text-cyan-400 font-mono text-sm mb-2">Name</label>                   <input type="text" className="w-full bg-black border border-gray-700 p-3 text-white focus:border-fuchsia-500 focus:outline-none transition-colors" placeholder="Who are you?" />                 </div>                 <div className="text-left">                   <label className="block text-cyan-400 font-mono text-sm mb-2">Email</label>                   <input type="email" className="w-full bg-black border border-gray-700 p-3 text-white focus:border-fuchsia-500 focus:outline-none transition-colors" placeholder="email@address.com" />                 </div>               </div>               <div className="text-left">                 <label className="block text-cyan-400 font-mono text-sm mb-2">Idea</label>                 <textarea rows="4" className="w-full bg-black border border-gray-700 p-3 text-white focus:border-fuchsia-500 focus:outline-none transition-colors" placeholder="Describe your dream piece. Colors, vibe, shape..."></textarea>               </div>               <NeonButton color="pink" className="w-full">Send Request</NeonButton>             </form>           </div>            {/* Socials */}           <div className="mt-16 flex justify-center space-x-8">             <a href="#" className="text-gray-500 hover:text-fuchsia-500 hover:scale-125 transition-all"><Instagram size={32} /></a>             <a href="#" className="text-gray-500 hover:text-cyan-400 hover:scale-125 transition-all"><Facebook size={32} /></a>             <a href="#" className="text-gray-500 hover:text-white hover:scale-125 transition-all"><Mail size={32} /></a>           </div>           <p className="mt-8 text-gray-700 font-mono text-xs">© 2024 Hollywood Riot Resin. All Rights Reserved.</p>         </div>       </section>     </div>   ); } 
